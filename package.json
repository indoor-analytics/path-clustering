{
  "name": "@indoor-analytics/path-clustering",
  "version": "0.0.1",
  "description": "Clusters a bunch of paths with graph representation.",
  "main": "main.js",
  "types": "main.d.ts",
  "scripts": {
    "build": "npx tsc",
    "clean": "find . -regex '.*\\.\\(js\\|d.ts\\)' -not -path \"./node_modules/*\" -type f -delete",
    "prepare": "npm run build",
    "test": "npx ts-mocha test/tests/*.test.ts"
  },
  "author": "Rémy Raes",
  "license": "ISC",
  "devDependencies": {
    "@turf/helpers": "^6.3.0",
    "@types/expect": "^24.3.0",
    "@types/mocha": "^8.2.2",
    "chai": "^4.3.4",
    "ts-mocha": "^8.0.0",
    "typescript": "^4.2.4"
  }
}
